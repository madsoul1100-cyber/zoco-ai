services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - zoco-mongo:/data/db
    command: ["mongod", "--quiet"]

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - zoco-redis:/data

volumes:
  zoco-mongo:
  zoco-redis:
