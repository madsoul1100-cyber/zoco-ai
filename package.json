{
  "name": "zoco-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Zoco AI — self-hosted voice calling platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix realtime",
    "infra": "docker compose up -d",
    "infra:down": "docker compose down",
    "dev": "concurrently -n api,web,livekit,pipecat -c cyan,magenta,green,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix realtime\" \"bash scripts/run-pipecat.sh\"",
    "dev:web": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:worker": "npm run dev --prefix realtime",
    "dev:pipecat": "bash scripts/run-pipecat.sh",
    "dev:all": "npm run dev",
    "start": "npm run start --prefix backend",
    "clean:disk": "bash scripts/clean-disk.sh",
    "pm2:start": "npm run build --prefix frontend && pm2 startOrReload ecosystem.config.cjs --update-env && pm2 save",
    "pm2:restart": "pm2 restart zoco-api zoco-web zoco-worker zoco-pipecat",
    "pm2:stop": "pm2 stop zoco-api zoco-web zoco-worker zoco-pipecat",
    "pm2:logs": "pm2 logs zoco-api zoco-web zoco-worker zoco-pipecat"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "livekit-client": "^2.22.1"
  }
}
