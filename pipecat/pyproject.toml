[project]
name = "zoco-pipecat-worker"
version = "0.1.0"
description = "Zoco Pipecat voice worker"
requires-python = ">=3.11"
dependencies = [
  "pipecat-ai[runner,webrtc,daily,silero,deepgram,openai,cartesia]>=1.0.0",
  "python-dotenv>=1.0.1",
  "aiohttp>=3.10.0",
]

[tool.uv]
package = false
